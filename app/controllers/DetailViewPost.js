var args = arguments[0] || {};

//recibe los argumentos y despliega el detalle de la publicación

$.imagen.image = args.imate || '';
$.texto.text = args.text || '';

function closeDetail() {
	//Cierra la ventana o cambia el stack de la vista a la principal
		
}
